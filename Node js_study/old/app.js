const fs = require("fs");
// 파일에 내용 불러오기 및 인코딩으로 값 변경
//const data = fs.readFileSync("./test.txt", {encoding: "utf-8"});

//console.log(data);

// 파일에 내용추가
//fs.appendFileSync("./test.txt","\nWelcome");

// 경로안에 있는 파일 불러오기
//const list = fs.readdirSync(".");
//console.log(list);